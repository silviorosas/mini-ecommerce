spring:
  application:
    name: notificaciones-service
  config:
    import: "optional:configserver:http://config-server:8888/"

  # Esta línea es opcional pero ayuda a que el servicio
  # no falle si el Config Server tarda 2 segundos más en subir
  cloud:
    config:
      fail-fast: false
